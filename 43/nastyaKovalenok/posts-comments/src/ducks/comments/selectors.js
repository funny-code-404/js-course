export const dataCommentsSelector = (state) => state.comments.data;

export const commentsErrorSelector = state => state.comments.error;
export const isLoadingCommentsSelector = state => state.comments.isLoading;
