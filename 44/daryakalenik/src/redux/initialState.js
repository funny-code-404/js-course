import initialPokemonState from "../ducks/pokemons/initialState";
import initialPokemonDetailsState from "../ducks/pokemonDetails/initialState";

const initialState = {
  data: initialPokemonState,
  details: initialPokemonDetailsState,
};

export default initialState;
